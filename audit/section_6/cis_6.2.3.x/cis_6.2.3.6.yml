---

{{ if .Vars.bozkarcis_level_1 }}
  {{ if .Vars.bozkarcis_rule_6_2_3_6 }}
    {{ if not .Vars.bozkarcis_remote_log_server }}
command:
  remote_syslog:
    title: 6.2.3.6 | Ensure rsyslog is configured to send logs to a remote log host
    exec: 'grep -E "action.*omfwd.*target" /etc/rsyslog.conf /etc/rsyslog.d/*.conf'
    exit-status:
      or:
        - 0
        - 2
    stdout:
    - '*.* action(type="omfwd" target="{{ .Vars.bozkarcis_remote_log_host }}" port="{{ .Vars.bozkarcis_remote_log_port }}"'
    - protocol="{{ .Vars.bozkarcis_remote_log_protocol }}"
    meta:
      server: 1
      workstation: 1
      CIS_ID:
      - 6.2.3.6
      NIST800-53R5:
      - AU-6
    {{ end }}
  {{ end }}
{{ end }}
